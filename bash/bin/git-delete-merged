#!/bin/bash

git branch --merged | grep -v master | xargs git branch -d
git branch -r --merged | awk -F'/' '/^ *origin/{if (!match($0, /(>|master)/) || (!match($0, /(>|staging)/) || (!match($0, /(>|production)/)) {print $2}}' | xargs git push origin --delete
